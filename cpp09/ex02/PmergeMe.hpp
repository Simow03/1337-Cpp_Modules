#ifndef PMERGEME_HPP
#define PMERGEME_HPP

#include <iostream>
#include <cstdlib>
#include <climits>
#include <vector>
#include <algorithm>

bool isPositiveInteger(std::string input);
std::vector<int> mergeInsertionSort(std::vector<int>& numbers);

#endif